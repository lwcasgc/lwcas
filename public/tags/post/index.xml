<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Post on lwcas</title>
    <link>http://localhost:1313/tags/post/</link>
    <description>Recent content in Post on lwcas</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-US</language>
    <managingEditor>lwcasgc@gmail.com (lwcas)</managingEditor>
    <webMaster>lwcasgc@gmail.com (lwcas)</webMaster>
    <copyright>lwcas</copyright>
    <lastBuildDate>Fri, 11 Jul 2025 00:00:00 +0000</lastBuildDate>
    <atom:link href="http://localhost:1313/tags/post/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>rust things i gotta remember #1: borrow</title>
      <link>http://localhost:1313/blog/blog_post/</link>
      <pubDate>Fri, 11 Jul 2025 00:00:00 +0000</pubDate><author>lwcasgc@gmail.com (lwcas)</author>
      <guid>http://localhost:1313/blog/blog_post/</guid>
      <description>&lt;h1 id=&#34;what-is-borrow-in-rust&#34;&gt;what is &amp;ldquo;borrow&amp;rdquo; in rust?&lt;/h1&gt;&#xA;&lt;p&gt;borrow in rust is a way to use a value without taking ownership of it. instead of moving a value from one place to another, you can &amp;ldquo;borrow&amp;rdquo; it using references. this helps rust ensure memory safety without needing a garbage collector.&lt;/p&gt;&#xA;&lt;h2 id=&#34;a-simple-analogy&#34;&gt;a simple analogy&lt;/h2&gt;&#xA;&lt;p&gt;think of a book on your table:&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;ownership is like being the owner of the book.&lt;/li&gt;&#xA;&lt;li&gt;borrowing is like letting someone else look at or use the book without giving them ownership. the owner still has the book afterwards.&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h2 id=&#34;how-it-works-in-practice&#34;&gt;how it works in practice&lt;/h2&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;immutable borrow (reading): you borrow only to read. in rust this is &lt;code&gt;&amp;amp;&lt;/code&gt;.&lt;/li&gt;&#xA;&lt;li&gt;mutable borrow (changing): you borrow to modify. in rust this is &lt;code&gt;&amp;amp;mut&lt;/code&gt;.&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;p&gt;simple abstracted examples (variable names show roles):&lt;/p&gt;</description>
      <content:encoded><![CDATA[<h1 id="what-is-borrow-in-rust">what is &ldquo;borrow&rdquo; in rust?</h1>
<p>borrow in rust is a way to use a value without taking ownership of it. instead of moving a value from one place to another, you can &ldquo;borrow&rdquo; it using references. this helps rust ensure memory safety without needing a garbage collector.</p>
<h2 id="a-simple-analogy">a simple analogy</h2>
<p>think of a book on your table:</p>
<ul>
<li>ownership is like being the owner of the book.</li>
<li>borrowing is like letting someone else look at or use the book without giving them ownership. the owner still has the book afterwards.</li>
</ul>
<h2 id="how-it-works-in-practice">how it works in practice</h2>
<ul>
<li>immutable borrow (reading): you borrow only to read. in rust this is <code>&amp;</code>.</li>
<li>mutable borrow (changing): you borrow to modify. in rust this is <code>&amp;mut</code>.</li>
</ul>
<p>simple abstracted examples (variable names show roles):</p>
<p>immutable borrow (abstract names):</p>





<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="ln">1</span><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="n">owner</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s">&#34;hello&#34;</span><span class="p">;</span><span class="w">     </span><span class="c1">// the original value
</span></span></span><span class="line"><span class="ln">2</span><span class="cl"><span class="c1"></span><span class="kd">let</span><span class="w"> </span><span class="n">reader1</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="n">owner</span><span class="p">;</span><span class="w">    </span><span class="c1">// borrowed to read
</span></span></span><span class="line"><span class="ln">3</span><span class="cl"><span class="c1"></span><span class="kd">let</span><span class="w"> </span><span class="n">reader2</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="n">owner</span><span class="p">;</span><span class="w">    </span><span class="c1">// another read-only borrow
</span></span></span><span class="line"><span class="ln">4</span><span class="cl"><span class="c1"></span><span class="fm">println!</span><span class="p">(</span><span class="s">&#34;</span><span class="si">{}</span><span class="s">&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">reader1</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="ln">5</span><span class="cl"><span class="w"></span><span class="fm">println!</span><span class="p">(</span><span class="s">&#34;</span><span class="si">{}</span><span class="s">&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">reader2</span><span class="p">);</span></span></span></code></pre></div><p>mutable borrow (abstract names):</p>





<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="ln">1</span><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">container</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="fm">vec!</span><span class="p">[</span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="mi">2</span><span class="p">];</span><span class="w"> </span><span class="c1">// owner of a collection
</span></span></span><span class="line"><span class="ln">2</span><span class="cl"><span class="c1"></span><span class="kd">let</span><span class="w"> </span><span class="n">modifier</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="k">mut</span><span class="w"> </span><span class="n">container</span><span class="p">;</span><span class="w">  </span><span class="c1">// exclusive borrow to modify
</span></span></span><span class="line"><span class="ln">3</span><span class="cl"><span class="c1"></span><span class="n">modifier</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="mi">3</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="ln">4</span><span class="cl"><span class="w"></span><span class="fm">println!</span><span class="p">(</span><span class="s">&#34;</span><span class="si">{:?}</span><span class="s">&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">modifier</span><span class="p">);</span></span></span></code></pre></div><p>more abstract &amp;mut example showing immediate update and scope:</p>





<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="ln">1</span><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">value</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="mi">5</span><span class="p">;</span><span class="w">             </span><span class="c1">// owner
</span></span></span><span class="line"><span class="ln">2</span><span class="cl"><span class="c1"></span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="ln">3</span><span class="cl"><span class="w">    </span><span class="kd">let</span><span class="w"> </span><span class="n">modifier</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="k">mut</span><span class="w"> </span><span class="n">value</span><span class="p">;</span><span class="w"> </span><span class="c1">// exclusive borrow starts
</span></span></span><span class="line"><span class="ln">4</span><span class="cl"><span class="c1"></span><span class="w">    </span><span class="o">*</span><span class="n">modifier</span><span class="w"> </span><span class="o">+=</span><span class="w"> </span><span class="mi">1</span><span class="p">;</span><span class="w">            </span><span class="c1">// updates happen immediately in memory
</span></span></span><span class="line"><span class="ln">5</span><span class="cl"><span class="c1"></span><span class="w">    </span><span class="fm">println!</span><span class="p">(</span><span class="s">&#34;</span><span class="si">{}</span><span class="s">&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">modifier</span><span class="p">);</span><span class="w">  </span><span class="c1">// prints 6
</span></span></span><span class="line"><span class="ln">6</span><span class="cl"><span class="c1"></span><span class="p">}</span><span class="w"> </span><span class="c1">// modifier goes out of scope here (mutable borrow ends)
</span></span></span><span class="line"><span class="ln">7</span><span class="cl"><span class="c1"></span><span class="fm">println!</span><span class="p">(</span><span class="s">&#34;</span><span class="si">{}</span><span class="s">&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">value</span><span class="p">);</span><span class="w">         </span><span class="c1">// prints 6 — owner can be accessed now
</span></span></span></code></pre></div><p>example that will not compile (shows rule about using owner while &amp;mut exists):</p>





<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="ln">1</span><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">value</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="mi">5</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="ln">2</span><span class="cl"><span class="w"></span><span class="kd">let</span><span class="w"> </span><span class="n">modifier</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="o">&amp;</span><span class="k">mut</span><span class="w"> </span><span class="n">value</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="ln">3</span><span class="cl"><span class="w"></span><span class="fm">println!</span><span class="p">(</span><span class="s">&#34;</span><span class="si">{}</span><span class="s">&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">value</span><span class="p">);</span><span class="w"> </span><span class="c1">// error: cannot borrow `value` as immutable while `modifier` (a mutable borrow) exists
</span></span></span></code></pre></div><h2 id="why-these-rules-exist">why these rules exist</h2>
<p>these rules prevent common mistakes like using something that was already removed or modifying something at the same time (which can cause unexpected behavior). they help rust guarantee <strong>memory safety</strong> at compile time.</p>
<p><strong>memory safety</strong> means a program can&rsquo;t read or write memory it shouldn&rsquo;t. this prevents common bugs like:</p>
<ul>
<li>
<p>use-after-free: reading data after it was dropped</p>
<ul>
<li>can cause undefined behavior: crashes, corrupted memory, or wrong values</li>
<li>security risk: may allow attackers to execute code or disclose sensitive data</li>
<li>typically timing-dependent and hard to reproduce, making bugs elusive</li>
</ul>
</li>
<li>
<p>double-free: freeing the same memory twice</p>
<ul>
<li>leads to heap/allocator corruption and undefined behavior, often crashing the program</li>
<li>can corrupt allocator metadata so future allocations return invalid pointers</li>
<li>a common vector for exploitation (control-flow hijack or arbitrary code execution)</li>
</ul>
</li>
<li>
<p>data races: two threads changing the same data at once</p>
<ul>
<li>causes nondeterministic behavior, lost updates, and inconsistent reads (corrupted state)</li>
<li>can produce crashes, deadlocks, or subtle logic errors that are hard to reproduce</li>
<li>may break program invariants and create security issues when sensitive data is involved</li>
</ul>
</li>
</ul>
<p>rust enforces memory safety at compile time using ownership, borrowing, and lifetimes:</p>
<ul>
<li>ownership: one owner is responsible for cleaning up a value, so it won&rsquo;t be freed unexpectedly</li>
<li>borrows: references let others read or modify without taking ownership, and the rules prevent conflicting access</li>
<li>lifetimes: the compiler ensures references don&rsquo;t outlive the data they point to</li>
</ul>
<p>the result: many memory errors are caught before the program runs, so code is safer without needing a garbage collector.</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
